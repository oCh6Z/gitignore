#   Add = Create a capability.
#   Drop = Delete a capability.
#   Fix = Fix an issue.
#   Bump = Increase the version of something.
#   Make = Change the build process, or tools, or infrastructure.
#   Start = Begin doing something.
#   Stop = End doing something.
#   Optimize = A change that MUST be just about performance.
#   Document = A change that MUST be only in the documentation.
#   Refactor = A change that MUST be just refactoring.
#   Reformat = A change that MUST be just format.
#   Rephrase = A change that MUST be just textual.
#
#   If applied, this commit will...

#   Why is this change happening?

#   Tags suitable for searching.
